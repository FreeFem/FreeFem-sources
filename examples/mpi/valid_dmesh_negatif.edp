// NBPROC 2
// PARAM -wg -ns -ffddm_overlap 1
include "ffddm.idp"
load "PETSc"

meshS Th = square3(10, 10);
DmeshS DTh = distribute(Th, overlap = ffddmoverlap);

fespace Udh(DTh, [P2,P1]);     // composite/mixte : doit lever ExecError ici-meme
cout << "[ECHEC DU TEST] cette ligne ne doit jamais s'afficher" << endl;
