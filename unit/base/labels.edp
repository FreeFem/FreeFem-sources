load "msh3"
include "test.idp"

cout << "labels" << endl;

real t;
mesh Th = square(10, 10);
mesh3 Th3 = cube(10, 10, 10);

func int getsize(int[int] array) {
  int i;
  for (i = 0; array[i] != "\0"; i++);
  return i + 1;
}

t = clock();
int[int] res = labels(Th);
t = clock() - t;
cout << IND << t << endl;
cout << res << endl;

t = clock();
res = labels(Th3);
t = clock() - t;
cout << IND << t << endl;
