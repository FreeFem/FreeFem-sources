include "test.idp"

cout << "arealevelset" << endl;

real t;
real[int, int] arrayr = [[1, 2, 3], [4, 5, 6]];
real r = 1;
mesh Th;

t = clock();
// arealevelset(Th, arrayr, r);
t = clock() - t;
cout << IND << t << endl;
