//  bug string macro parameter   version < 1.41
// -----------------
 macro tyty(uu) uu//  
cout << tyty("toto") << endl;

// other bug   un func parameter  no correct 

  func bool  write(string  fn,real[int] & u)
    {
      cout << fn << endl;  //  delete 2 times
      return true; 
    }

  func bool  write1(string &  fn,real[int] & u)
    {
      cout << fn << endl; //  
      return true; 
    }

 real[int] u(10);
 u=1;
string tt="toto1";

 write1(tt,u);

 write("toto",u);
