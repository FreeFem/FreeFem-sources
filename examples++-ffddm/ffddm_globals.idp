
int npart = mpisize; 


/*

Vh<K> def(un) = minit(0), def(sol) = minit(0), def(ver);            // initial guess, final solution and error

Vh<K> def(rhsglobal),def(uglob);         // rhs and solution of the global problem

fespace Vhg1(Th,P1);

Vhg1 ddmsol;

*/








  
  


  //fespace Vhz(Th,Pk); // comme Vh!?
  //Vhi<K>[int] def(Z)(ncoarsespacemax);
  //K[int,int] Ef(ncoarsespace,ncoarsespace), Efmc(ncoarsespace,ncoarsespace);

  
  int giter;
  

