Version en cours de developpement.
This run only on MacOs 10.2 version.
This version use OpenGL graphics.

This version can by by in 2 mode
like a Mac application with drap and drop, ...
This application open a terminal window to make console IO.

This application must by in the /Applications directory if 
you want to use this application in a terminal. You have jsut to put to 
script FreeFem++-CoCoa in a directory of of shell PATH.
