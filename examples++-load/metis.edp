load "metis"
mesh Th=square(50,50);
fespace Ph(Th,P0);
int[int] nupart(Th.nt);
Ph  part;

metisnodal(nupart,Th,10);
for(int i=0;i<nupart.n;++i)
  part[][i]=nupart[i];
plot(part,fill=1);

metisdual(nupart,Th,10);
for(int i=0;i<nupart.n;++i)
  part[][i]=nupart[i];
plot(part,fill=1);