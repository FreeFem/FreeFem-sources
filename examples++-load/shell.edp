load "shell"
Directory  c=".";

while(1)
{
	string n=readdir(c);
	if( n != "") 
	  cout << n << " " << isdir(n) << endl; 
	else break; 
}

cout << " login " << getenv("LOGIN") << endl;
setenv("FF_TOTO","toto");
setenv("FF_TOTO","toto");
cout << " FF_TOTO  " << getenv("FF_TOTO") << endl;
